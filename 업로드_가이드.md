# 📤 GitHub 업로드 가이드

## 🎯 목표
- 기존 파일 정리
- 등락률 상위 시스템 추가
- GitHub Actions 설정

---

## 🚀 빠른 업로드 (추천)

### Windows 사용자
```bash
# GitHub_업로드.bat 더블클릭
```

---

## 📝 수동 업로드 방법

### 1단계: Git 설정 확인
```bash
cd "C:\Users\USER\Desktop\issue automation"

# 원격 저장소 확인
git remote -v

# 없으면 추가
git remote add origin https://github.com/dongsuki/issue-automation.git
```

### 2단계: 모든 파일 추가
```bash
# 모든 변경사항 추가
git add .

# 상태 확인
git status
```

### 3단계: 커밋 생성
```bash
git commit -m "feat: 등락률 상위 시스템 추가 및 GitHub Actions 설정"
```

### 4단계: GitHub에 푸시
```bash
# 메인 브랜치로 푸시
git push origin main

# 만약 에러 나면 강제 푸시 (기존 파일 덮어쓰기)
git push -f origin main
```

---

## ⚠️ 기존 파일 완전히 지우고 새로 시작하려면

### 방법 1: 레포지토리 삭제 후 재생성
1. GitHub 사이트에서 레포지토리 삭제
   - Settings → Danger Zone → Delete this repository
2. 새 레포지토리 생성
3. 아래 명령어 실행:

```bash
cd "C:\Users\USER\Desktop\issue automation"

# 기존 .git 폴더 삭제
rmdir /s /q .git

# Git 초기화
git init
git add .
git commit -m "Initial commit: 급등이슈 & 등락률 상위 자동화"

# 원격 저장소 연결
git remote add origin https://github.com/dongsuki/issue-automation.git
git branch -M main
git push -u origin main
```

### 방법 2: 강제 푸시 (기존 파일 덮어쓰기)
```bash
cd "C:\Users\USER\Desktop\issue automation"

git add .
git commit -m "feat: 전체 시스템 재구성 (등락률 상위 추가)"
git push -f origin main
```

---

## 🔐 중요: GitHub Secrets 설정

업로드 후 **반드시** 해야 할 작업:

### 1. service_account.json.json 파일 내용 복사
```bash
# 파일 열어서 전체 내용 복사
notepad service_account.json.json
```

### 2. GitHub Secrets 등록
1. https://github.com/dongsuki/issue-automation
2. **Settings** 탭 클릭
3. 왼쪽 **Secrets and variables** → **Actions**
4. **New repository secret** 클릭
5. Name: `GOOGLE_CREDENTIALS`
6. Value: 복사한 JSON 내용 붙여넣기
7. **Add secret** 클릭

---

## ✅ 업로드 후 확인사항

### 1. 파일 확인
- [ ] `.github/workflows/급등이슈_생성.yml`
- [ ] `.github/workflows/등락률상위_생성.yml`
- [ ] `main.py` (급등이슈)
- [ ] `main_ranking.py` (등락률 상위)
- [ ] `template.html` (급등이슈)
- [ ] `template_ranking.html` (등락률 상위)
- [ ] `README.md`, `GitHub_사용법.md`, `다운로드_방법.md`

### 2. .gitignore 확인
```
service_account.json.json  ✅ 절대 업로드 금지!
```

### 3. GitHub Actions 활성화
1. **Actions** 탭
2. "I understand my workflows, go ahead and enable them" 클릭

---

## 🎯 업로드 후 테스트

### 1. 수동 실행 테스트
1. **Actions** 탭
2. **"급등이슈 카드 자동 생성"** 선택
3. **Run workflow** 클릭
4. 완료 대기 (1~2분)

### 2. Artifacts 다운로드
1. 완료된 워크플로우 클릭
2. 하단 **Artifacts** 섹션
3. **📦 급등이슈_이미지** 다운로드 (ZIP)

---

## 🔍 문제 해결

### "Permission denied" 에러
```bash
# GitHub 로그인 확인
git config --global user.name "dongsuki"
git config --global user.email "your-email@example.com"

# Personal Access Token 사용
# GitHub → Settings → Developer settings → Personal access tokens
```

### "Failed to push" 에러
```bash
# 강제 푸시
git push -f origin main
```

### Actions가 실행 안 됨
- Settings → Actions → General
- **Allow all actions and reusable workflows** 선택

---

## 📞 완료 체크리스트

- [ ] 모든 파일 업로드 완료
- [ ] `GOOGLE_CREDENTIALS` Secret 설정 완료
- [ ] Actions 활성화 완료
- [ ] 테스트 실행 성공
- [ ] Artifacts 다운로드 성공

**완료되면 https://github.com/dongsuki/issue-automation에서 확인!** 🎉

